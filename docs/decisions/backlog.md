# Backlog — Work Source

The single source of work for the autonomous system. Seeded from the
"Current priorities" of `docs/PROJECT.md` and the build plan in
`docs/HANDOFF.md`; ad-hoc operator asks are added here too. `/work-next`
takes the topmost open item.

Mark items `- [ ]` (open) or `- [x]` (done). Keep the most important open
item on top. New findings are surfaced here or to the queue — never
silently appended elsewhere.

All build work happens on `dev`; finished tools promote to `main` one at a
time (see `docs/HANDOFF.md` §2).

## Open — furniture fit + CAD basis (operator-directed 2026-07-19, first drafts landed)

- [ ] **Furniture Fit: operator taste pass on the first draft.** `l/furniture.html` + `l/lib/` are built, noindexed, smoke-tested (23 jsdom checks; module suites 80 + 24). Real-device touch behavior (iPad drag/pinch feel, snap radii) needs hands-on review before anything promotes.
- [ ] **Fit tool: tiered clearance ranges + comfort setting.** The research's strongest v2 abstraction (docs/In Progress/furniture-field-scan.md): every clearance becomes {min, comfortable, accessible}, one global comfort level resolves all zones, and checks report *which tier* a layout meets. Today the catalog draws the comfortable tier and notes the minimum in prose.
- [ ] **Fit tool: layout templates (whole-room blocks).** Queen bedroom w/ closet variants, sofa-facing-sofa, conference small/medium — assembled from catalog objects, mirroring dimensions.com's classification > collection > element structure. Doors/windows on the room boundary come first (swing zones join the conflict sweep).
- [ ] **Fit tool: undo stack + two-finger-tap undo, marking menu, snap-and-go.** The touch-language items specced in docs/In Progress/touch-cad-ui.md §9, in priority order.
- [ ] **CAD basis: interval-on-curve node.** The piece that unlocks trim/extend/fillet/break as re-evaluable nodes (docs/In Progress/cad-basis.md roadmap #1). Then the depth policy (LatticePoint vs RatPoint types), offset, mirror-about-rational-line, dimension nodes.
- [ ] **CAD basis: the feet-inch keypad on the demo page.** The Construction-Master-anatomy keypad (touch-cad-ui.md §7) as a reusable piece over parse-length — the Fit tool's dim fields adopt it after.

## Open — calculator engine (the active thread)

- [ ] **Promote the new tools to `main` one at a time, after operator review.** Eleven new engine tools plus the C-0 index are built on `dev`, noindexed, gate-green, jsdom-smoke-tested (185 checks), AND now rendered in real headless Chromium (14 pages × light/dark × desktop/phone: zero page errors, zero overflow, dark figures inverting). What remains before promotion is the operator's taste pass. Per HANDOFF §2: each page moves to `main` in a small commit (drop its `noindex`; wire convert.html's crumb to C-0 when the index promotes).
- [ ] **Plumbing fixtures: replace the draft table values with checked ones.** The page is BUILT (banded engine + §403.1.1 split, tested); business WC/lav bands and A-1 theater WC ratios are verified against ICC source material and tagged so. Every other ratio renders a visible 'draft — check the table' tag. Replace drafts from an operator-checked copy of IPC Table 403.1, flip their `v:` flags, and update the rows in `docs/REFERENCES.md` — the citation ledger and the page tags must agree.
- [ ] **Exits: single-exit allowances + travel distances are pointers, not rows.** The count thresholds and half-diagonal separation are verified and computed; Table 1006.2.1 (single-exit) and the travel/common-path/dead-end tables are per-occupancy and stayed out rather than half-remembered. Add them the fixtures way (per-value provenance tags) from a checked copy, with `docs/REFERENCES.md` rows in the same commit.
- [ ] **Add a favicon.** Surfaced by the Chromium sweep: every page 404s on /favicon.ico. Needs a mark decision — operator call.

## Open — engine/convert review follow-ups (lower priority)

These came out of the `convert.html` + lib review. The engine-level items
(single-source rounding kernel, public-input validation, infeasibility `reason`
codes, range docs) are **done**; what remains is the converter UI and a parser
extension. The converter is feature-frozen, so these are deliberate, not urgent.

- [ ] **Parser: stable `reason`/`errorCode` for unresolved states.** Source: review #4. Distinguish division-by-zero, conflicting adjacent units, missing operand, mixed area/length, etc., so the UI renders copy from a code (mirrors what `partition` now does for infeasible layouts). Touches the frozen parser — scope carefully.
- [ ] **convert.html: decide pending-span policy + area/ratio polish.** Source: review #3 / usability #5. Keep salvage-parse for the converter but make the "ignoring …" note prominent; consider a `strict` flag for future code/check calculators; either label area/ratio as bonus outputs or give them the length path's formatting.

## Open — provenance / hosting (loose ends from HANDOFF §4)

- [ ] **Stand up a neutral preview host + `docs/staging.md`.** Source: HANDOFF.md §4. Recommended: Cloudflare Pages on its `*.pages.dev` URL (auto-noindexed; do not attach the custom domain). Never serve WIP under `architecture.tools` or a subdomain of it.

## Open — site structure (lower priority)

- [ ] **Richer AT-0 master cover (deferred behind picker-only launch).** On `main`, `index.html` *is* the picker. The fuller AT-0 cover — site thesis, a compact index of the sets, links to flagship pages — lands when more of the site is promoted.
- [ ] **Bring the last pages onto the shared stylesheet.** Source: PROJECT.md. Every `calculators/` page except `parking-ratio.html` is on `assets/css/global.css` + `tokens.css` now. Remaining: `parking-ratio.html` (or retire it — zoning-adjacent, default-out), `site-screen.html` (own inline styles), and a review pass of `components.html` (linked, never reviewed against the theme).
- [ ] **Add thin A-series pages gradually (A-0, then A-1 … A-4).** Source: PROJECT.md / SITE_FRAMEWORK build order. A-1 Workstation Types, A-2 Monitors, A-3 Mobile First Setups, A-4 AI in Architecture.

## Done

- [x] **Furniture Fit tool — first draft (`l/furniture.html` + `l/lib/catalog.js` + `l/lib/fitmath.js`).** Operator ask 2026-07-19. The L-series' first working page: parametric furniture makers (not blocks) with clearances as included snap-and-warn geometry. 15 objects across bedroom/dining/living/office/breakroom/retail — bed set with the six mattress variants + custom + nightstand rule, dining rect/round with derived seats (24″/person), sofa/armchair/coffee/console, conference (30″/person, 48″ zone), work cubes, bench desks (b2b toggle), kitchenette (NKBA 42/48 aisles), café tables, gondola runs (48″ sections, Lozier 13/16/19/22 depths, facings-per-density math), checkout, garment rack. Wall intelligence lives in the catalog: a table at a wall drops that side's chairs + zone, a bed drops walkway + nightstand, a gondola collapses to single-sided, a dresser WARNS (drawers face the wall); sofas/consoles auto-orient back-to-wall on drop. Touch-first canvas (commit-on-lift drag with grab offset, snap to furniture/zones/walls with guide flashes, wider touch apertures, double-tap rotate, pinch/pan) + the panel as one declarative parameter language (variant pills / dim wells on parse-length / steppers / toggles) with exact place fields and coarse/fine nudges. Conflicts surfaced, never blocking (collision / zone encroachment / wall pinch). Shareable URL state. Every load-bearing dimension cross-checked in the research pass (field-scan doc + REFERENCES.md rows for ADA 403.5.1, NKBA). 80 + 24 module tests, 23 jsdom checks.
- [x] **CAD basis — the exact kernel (`cad/lib/` + `cad/index.html`).** Operator ask 2026-07-19: the basis of a browser 2D CAD on the exact integer engine, holding remainders for late conversion and exact transforms/geometry. Four pure modules, 102 inline tests: `rat.js` (BigInt rationals over the 1/960 mm lattice; toLattice returns snapped integer + EXACT remainder; rounding-policy parity with snap.js proven by test), `plane.js` (exact transforms; rotation = rational half-angle tangent — t=1 is exactly 90°, t=1/3 the 3-4-5 rotation; arbitrary angles quantized once at input like parse-length; 100 composed transforms invert bit-identically), `geom2.js` (orient as a true sign, exact segment intersections that substitute back to orientation 0, exact point-in-polygon with a real 'edge' answer, outward lattice bbox with remainders), `model.js` (the tile module's node understanding generalized: insert = powercopy with declared inputs, array with exact per-instance parameter tweens, fitArray reproducing partition.js count/residual/reason-code exactly on rationals, hatch with every endpoint EXACTLY on the boundary + edge bands as residuals). Demo page proves it live with a watertightness counter. Research-validated architecture (CGAL lazy-exact, snap rounding, Clipper2 precedent, Niven's theorem) + deviations recorded in docs/In Progress/cad-basis.md. calculators/lib untouched.
- [x] **Research pass: furniture field scan, dimension verification, touch CAD UI, exact geometry.** 7-agent sweep + adversarial verification (2026-07-19). Products: `docs/In Progress/furniture-field-scan.md` (paid-density map — CET/2020 four-figure parametrics, Social Tables' clearance-as-parameter, planogram ladder $400–$100k; the four-property gap; dimensions.com layout language; per-value dimension provenance), `docs/In Progress/touch-cad-ui.md` (the interaction language: commit-on-lift, Shift loupe, snap-and-go, 8-sector marking menus, quasimodes, two-hand arbiter, feet-inch keypad anatomy, arrays without dialogs), `docs/In Progress/cad-basis.md` (kernel architecture + roadmap: interval-on-curve, depth policy, snap-rounded export with an exactness report). REFERENCES.md gained verified rows (ADA 403.5.1, NKBA work aisles).

- [x] **Homeowner Questionnaire (`calculators/programming.html`) — print-first worksheet.** Operator ask, then clarified: a curated worksheet firms PRINT and hand out, not an interactive form. Part 2 of `docs/In Progress/project-kickoff-tools.md` voice-flipped into the owner-facing handout: the architect-side strategy (constraints before wishlist, program by verb, the hidden approver, want/need/dream with collisions kept on purpose) is encoded in the question selection and order; strategy notes stay out of the client's view. Paper does the work: ruled answer lines, printed checkbox squares, a fixed 12-row need/want/dream table with three mark columns, a fill-in intake strip (client/address/date/prepared-by); the only script is a print button. Print CSS re-bases the rem scale to document type (a stated print-medium exception to the 18px screen rule) and paginates to THREE letter pages, verified by rendering the actual PDF in Chromium. Building-family accent — first use. The first interactive cut (localStorage autosave + copy-summary) was built and then removed on the clarification — recoverable from history if a digital twin is ever wanted. Part 1 of the draft (the code-study wayfinder) stays in In Progress. 14 jsdom checks, render-swept + PDF-checked.

- [x] **Codes & Standards reference sheet (`calculators/codes.html`).** Operator ask: the list of all applicable codes and when they get used. The `docs/In Progress/spec-code-library.html` draft graduated onto the shared theme with its NCS series organization, edition pills, access tags, search/filter, and source links kept — plus a new "what applies when" section covering the model-code family (IBC vs IRC scoping, IEBC, IFC, trade codes + UPC/UMC note, IECC/90.1, NEC, NFPA 101, ADA as a federal overlay + FHA, A117.1, ASCE 7, zoning's deliberate exclusion). Accuracy pass: entries pointing at built tools now link to them; unbuilt ones carry an honest "planned" tag. Cross-noted with the internal ledger (REFERENCES.md). 15 jsdom checks, render-swept.

- [x] **Exits on the engine.** Count thresholds (3 at 501–1,000, 4 above 1,000 — verified via ICC sources this session) + the half-diagonal separation rule (1/2, 1/3 sprinklered — verified, 1007.1.1) computed from the plan extent, both fractions always shown, next-threshold rows, single-exit allowance left as a draft-tagged pointer. OL now chains to egress width, exits, AND fixtures. 19 jsdom checks.
- [x] **Plumbing Fixtures rebuilt on the engine, data layer two-tier.** `lib/bands.js` (banded code ratios, 18 tests) + the page: §403.1.1 50/50 split (verified wording), exact quotients beside round-ups, band arithmetic in the footnote. Verified-vs-draft provenance is per-value data rendered as tags; custom ratios are 'yours'. 16 jsdom checks.
- [x] **Sheet Sizes restyled onto the shared theme** (drafting family, ANSI series added, title-block width surfaced as an assumption, 'largest that fits' badge corrected from 'smallest'). Resolves its 'home' question for now: it stays a C reference. 11 jsdom checks.
- [x] **Real-browser rendering achieved.** Puppeteer + headless Chromium installed in the session sandbox; full sweep of all 14 pages × light/dark × desktop/phone. Zero page errors, zero horizontal overflow; visual review of every new page done. (The harness lives in /tmp — not committed; rebuildable from the state.md note.)

- [x] **Occupant Load + Egress Width rebuilt on the engine (suite map steps 5–6).** Both replace their pre-engine table-lookup pages, egress family accent, stairs' citation pattern throughout. OL: typed area takeoff ÷ a curated, cross-checked IBC 2021 Table 1004.5 factor set (legacy mercantile 30/60 corrected to the post-2015 flat 60 gross) + custom factor; gross/net basis echoed as an assumption; exact quotient / 'not less than' round-up / headroom rows; carries ?ol= into egress width (and area.html carries ?d= into OL). EW: load×factor exact in hundredths of an inch, capacity by exact integer floor-division; sprinkler condition a stated choice with the H/I-2 caveat; component minimums located with 'which number is larger' said plainly. 14 + 15 jsdom checks.
- [x] **Ramp on the engine (suite map family 4 — 'nearly free after slope').** Accessibility family accent. Rise × n exact; the 30" rise-per-run cap (405.6) segments long ramps via partition.sections with the drift surfaced; 60" landings counted (405.7) and drawn in plan by runbar; end landings added explicitly; handrail trigger (405.8) a fact row; alterations exceptions cited, never auto-applied. 13 jsdom checks.
- [x] **Slope: projected lengths (operator ask).** In-plan / in-elevation / along-the-slope rows; a solved leg leads and says which inputs governed; hypotenuse labelled on the figure; the one Pythagorean value's float-vs-lattice status stated in the footnote.
- [x] **C-0 calculator index + retire the superseded standalones.** Source: PROJECT.md / this backlog. `calculators/index.html` frames the set (one engine, the residual surfaced, no verdicts) and lists every page by lineage; the five new tools' crumbs route through it. Removed `dimension-converter.html`, `slope-calculator.html`, `stair-calculator.html` (all superseded, nothing linked them). `sheet-sizes.html` kept — see the new open item.
- [x] **Stairs on the engine (`stairs.html` + `lib/stairfig.js`).** Source: suite map step 2 — the bridge node. Every workable riser count is a row (riser, signed distance to the chosen reference's limit, run needed); edition pill + citation line (IRC 2021 R311.7.5 / IBC 2021 1011.5.2 + 'local amendments vary'); drift between n grid-risers and the true rise drawn hatched on a true-scale section; comfort rules located, labelled rules of thumb. 10 inline figure tests + 18 jsdom checks.
- [x] **Area + coverage tool on the engine (`area.html`).** Source: HANDOFF.md §5. The parser's expression evaluator already chains rooms and subtracts openings, so the takeoff is one typed expression; optional coverage divisor (itself an area expression, sheet chips) with both ends of the rounding shown; waste an explicit user choice echoed as an assumption. 18 jsdom checks.
- [x] **Slope tool on the engine (`slope.html` + `lib/slopefig.js`).** Source: HANDOFF.md §5. Any two of rise/run/slope (the echo says which two governed); one exact gcd-reduced rational rendered as %, ratio, pitch, degrees; bracketing standard steps with residuals expressed as rise over the user's run; common call-outs located, never ruled on; the angular residual hatched as a wedge in the true-angle triangle. 11 inline figure tests + 20 jsdom checks. Legacy `slope-calculator.html` + `stair-calculator.html` retired.
- [x] **Scale converter on the engine (`scale.html`).** Source: HANDOFF.md §5 (the quick win). Architect's / engineer's / metric / custom 1:n — all exact integer factors; drawn→real exact, real→drawn reports its lattice quantization; reading-precision dial reuses the numberline, and each snap residual also shows what it reads back as in real units through the scale. 13 jsdom checks.
- [x] **Run Solver — the partition tool page (`run.html` + `lib/runbar.js`).** Source: HANDOFF.md §5 step 1 (top of this backlog). Four modes over `partition.js` with architect-facing presets (tile/joint chips, 16" o.c., the 4" sphere reference named not enforced); floor/nearest/ceil rows with signed residuals; flush and balanced tile layouts as equal cards; joint relaxation reported as information with an ANSI A108.02 spec-check pointer. `runbar.js` (25 inline tests) draws the layout to scale, hatches the residual, and draws round-up OVERRUNS past the dimension line — found by the wiring smoke: sections/nearest can legitimately overrun, and the figure must show it rather than clamp. 22 jsdom checks.
- [x] **Retire `dimension-converter.html`.** Superseded by `convert.html`; removed with the other standalones above.
- [x] **Merge `main` into `dev` (operator-directed).** Operator: main's `index.html` picker + `convert.html` are the approved versions — get them into `dev`. Main carried the newer state of every overlapping file (promoted converter, consolidated css, hardened+clearance-fixed numberline, research-pass data, 06-09 docs); dev kept its unique files (`partition.js`, fixtures, HANDOFF, In Progress docs, deferred pages). Stale `global2.css` dropped. All suites + gate green.
- [x] **UI review pass + converter promotion.** Source: operator ("act on all of them"). One 18px base size site-wide; calculator shell graduated to global.css with labeled slots (HANDOFF §3); all pills became `<button>`s with `aria-pressed` + focus ring + coarse-pointer hit heights; picker DOM order = visual order, dead inline CSS trimmed; shared `assets/js/ui.js` (esc/press); converter URL state (`?d=&t=&g=`) + auto-flip echo note; footer date from `Meta.dataUpdated`; interim nav nailed (SITE_FRAMEWORK) and wired both ways; `convert.html` + lib promoted to `main` (noindex dropped). Verified: 22-check browser smoke (keyboard included), zero page errors, clip sweep still 0/9597, gate green.
- [x] **numberline: label clearance at narrow widths.** Source: operator report ("round down · nearest" clipped off the left in the 1 m imperial case on a phone). The bound blocks hug the hatch edges and extend outward; the side margins now reserve room for each block's widest line (conservative glyph estimate — the module is pure and can't measure), shrinking the magnified cell instead of losing text, with a per-label clamp as a final guarantee (true-value label included). Verified in-browser: 910 curated + 600 randomized figures, ~16k labels measured with getBBox, zero outside the viewBox (was 300). New inline regression test; all engine suites green (snap 19, numberline 13, partition 35).
- [x] **Consolidate to one global.css; single --paper definition.** Source: operator request. `global2.css` renamed over `assets/css/global.css` (the old cool-neutral sheet retired — the new sheet is a drop-in superset of its classes/tokens, so `components.html` and `dimension-converter.html` re-theme with zero edits). `--paper` is now defined only in `tokens.css` (the brand's warm-white anchor); the theme stopped re-declaring it light and dark, and the unused `--page-bg` alias was removed. Picker + converter verified computed-style identical in both schemes.
- [x] **Shared CSS for the picker + converter.** Source: operator request. `docs/In Progress/tokens.css` promoted to `assets/css/tokens.css` (the canonical palette); `global2.css` now imports it and owns the family-accent system (`<html data-family="…">` selects one of the seven family accents, light + dark) and the one shared dark scheme (surfaces / text / lines / shadows / number-line strokes). The duplicated `:root` mirrors and dark blocks were removed from `index.html` (family `picker`) and `convert.html` (family `drafting`). Verified pixel-identical in both schemes via headless-Chromium computed-style diff.
- [x] **convert.html UI fixes from the review.** Source: review usability #1/#3, #2. Fixed the mobile input-font selector (`.field-mono` → `#dimInput`); added an area out-of-range / non-finite guard parallel to the length guard (echo shows "area out of range", render skipped); reworded the Min/Max rows ("use for a max — won't exceed" / "use for a min — won't fall below") so "for a maximum" can't be read as "gives the maximum". Carry the clearer Min/Max labels into the partition tool page.
- [x] **Engine hardening from the convert.html review.** `numberline.js` + `partition.js` delegate the floor/nearest/ceil kernel to `snap.js` (one source of truth — removed the duplicated `snapTriple`/`pick`); `numberline()` validates its public inputs; `partition` infeasibility carries stable `reason` codes; range bound documented; conventions updated (HANDOFF §3). All three suites green (snap 19, numberline 12, partition 35).
- [x] **`partition()` engine module.** Source: HANDOFF.md §5. `calculators/lib/partition.js` — the layout primitive (sibling to `snap.js`, delegates its rounding kernel). Four modes (sections / tiles / on-center / balusters) behind one dispatcher; pure, fails loud, 35 inline `node` tests passing. The four tool pages are now thin layers on it (tracked above).
- [x] **Add `noindex` to the `dev` WIP pages.** Source: HANDOFF.md §4. `<meta name="robots" content="noindex, nofollow">` (with a "remove on promotion" comment) added to all 11 pages not yet on `main`: `components.html`, `site-screen.html`, and every `calculators/*.html`.
- [x] **Precise Unit Converter + calculator engine.** Source: HANDOFF.md. `calculators/lib/` (`parse-length`, `snap`, `numberline`) plus `convert.html` — shipped and frozen on `dev`.
- [x] **Reconcile the agent docs with the engine thread.** Source: HANDOFF.md §6. `state.md` and `backlog.md` refreshed: engine + converter folded in, removed/deferred pages corrected, CNAME/workflow + CSV pipeline facts fixed.
- [x] **Repo hygiene: remove committed `.DS_Store` files and ignore them.** Source: ad-hoc (noticed at bootstrap). Run as the system's end-to-end dry-run — Rung 1, scout → implement → reviewer → commit.
